import { createApp } from 'vue'
//import Vue from 'vue'
import App from './App.vue'
// import localString from './components/filtros/localString';

// Vue.filter('number', localString);

createApp(App).mount('#app')

// app.config.globalProperties.$filters = {
//     toLocal(val){
//         return val.toLocaleString('pt-bt');
//     }
// }
